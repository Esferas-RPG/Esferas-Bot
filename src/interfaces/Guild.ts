export interface GuildEmojiProps {
	guild: string;
	emoji: string;
	roleId: string;
}

export interface GuildRegisterRolesProps {
	noRegisteredRoleId: string;
	registeredRoleId: string;
	inicianteRoleId: string;
}
